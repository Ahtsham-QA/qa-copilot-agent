# QA Test Report
Generated: 2026-06-26 10:11:11
Generated by: QA Copilot Agent v2.0

---

## 📋 User Story
user can login with valid credentials

---

## 🧪 Test Cases
## TEST CASE 1: Successful Login with Valid Credentials
- Type: Positive
- Priority: High
- Preconditions: User has a registered account; application login page is accessible
- Test Data:
  * Email: john.doe@gmail.com
  * Password: SecurePass@123
- Steps:
  1. Navigate to the application login page
  2. Enter "john.doe@gmail.com" in the Email field
  3. Enter "SecurePass@123" in the Password field
  4. Click the "Login" button
- Expected Result: User is successfully logged in and redirected to the dashboard/home page; a welcome message or the user's name is displayed

---

## TEST CASE 2: Login Fails with Incorrect Password
- Type: Negative
- Priority: High
- Preconditions: User has a registered account; application login page is accessible
- Test Data:
  * Email: john.doe@gmail.com
  * Password: WrongPass@999
- Steps:
  1. Navigate to the application login page
  2. Enter "john.doe@gmail.com" in the Email field
  3. Enter "WrongPass@999" in the Password field
  4. Click the "Login" button
- Expected Result: Login is rejected; an error message is displayed such as "Invalid email or password"; user remains on the login page

---

## TEST CASE 3: Login Fails with Unregistered Email
- Type: Negative
- Priority: High
- Preconditions: Application login page is accessible; the email used does not exist in the system
- Test Data:
  * Email: notregistered@outlook.com
  * Password: AnyPass@456
- Steps:
  1. Navigate to the application login page
  2. Enter "notregistered@outlook.com" in the Email field
  3. Enter "AnyPass@456" in the Password field
  4. Click the "Login" button
- Expected Result: Login is rejected; an error message is displayed such as "Invalid email or password"; user remains on the login page

---

## TEST CASE 4: Login Fails with Empty Credentials
- Type: Negative
- Priority: High
- Preconditions: Application login page is accessible
- Test Data:
  * Email: (empty)
  * Password: (empty)
- Steps:
  1. Navigate to the application login page
  2. Leave the Email field blank
  3. Leave the Password field blank
  4. Click the "Login" button
- Expected Result: Form submission is blocked; validation error messages are shown under both fields such as "Email is required" and "Password is required"

---

## TEST CASE 5: Login with Email in Different Letter Cases
- Type: Edge Case
- Priority: Medium
- Preconditions: User has a registered account with email "john.doe@gmail.com"; application login page is accessible
- Test Data:
  * Email: JOHN.DOE@GMAIL.COM
  * Password: SecurePass@123
- Steps:
  1. Navigate to the application login page
  2. Enter "JOHN.DOE@GMAIL.COM" in the Email field
  3. Enter "SecurePass@123" in the Password field
  4. Click the "Login" button
- Expected Result: User is successfully logged in; the system treats the email as case-insensitive and grants access to the correct account

---

## TEST CASE 6: Login with Leading and Trailing Spaces in Email
- Type: Edge Case
- Priority: Medium
- Preconditions: User has a registered account with email "john.doe@gmail.com"; application login page is accessible
- Test Data:
  * Email: "   john.doe@gmail.com   " (with spaces before and after)
  * Password: SecurePass@123
- Steps:
  1. Navigate to the application login page
  2. Enter "   john.doe@gmail.com   " with leading and trailing spaces in the Email field
  3. Enter "SecurePass@123" in the Password field
  4. Click the "Login" button
- Expected Result: The system trims the spaces and logs the user in successfully, OR displays a clear validation error indicating the email format is invalid

---

## TEST CASE 7: Password Field Masks Input
- Type: Positive
- Priority: Medium
- Preconditions: Application login page is accessible
- Test Data:
  * Email: john.doe@gmail.com
  * Password: SecurePass@123
- Steps:
  1. Navigate to the application login page
  2. Click on the Password field
  3. Type "SecurePass@123" in the Password field
  4. Observe the characters displayed in the Password field
- Expected Result: Each character typed in the Password field is masked (displayed as dots or asterisks); the actual password is not visible in plain text

---

## TEST CASE 8: Login Page Redirects Already Logged-In User
- Type: Edge Case
- Priority: Low
- Preconditions: User is already logged into the application in the same browser session
- Test Data:
  * Email: john.doe@gmail.com
  * Password: SecurePass@123
- Steps:
  1. Log in successfully with valid credentials
  2. Without logging out, manually navigate back to the login page URL
- Expected Result: The application automatically redirects the already logged-in user to the dashboard/home page instead of displaying the login form again

---

## 🎭 Playwright Tests
```javascript
=== FILE: pages/LoginPage.js ===

```javascript
// LoginPage.js - Page Object Model for the SauceDemo Login Page

class LoginPage {
  constructor(page) {
    this.page = page;

    // Selectors using data-test attributes (preferred)
    this.usernameInput = page.locator('[data-test="username"]');
    this.passwordInput = page.locator('[data-test="password"]');
    this.loginButton = page.locator('[data-test="login-button"]');
    this.errorMessage = page.locator('[data-test="error"]');
    this.errorMessageContainer = page.locator('.error-message-container');

    // URL
    this.url = 'https://www.saucedemo.com';
  }

  // Navigate to the login page
  async navigate() {
    await this.page.goto(this.url);
  }

  // Enter username/email in the username field
  async enterUsername(username) {
    await this.usernameInput.clear();
    await this.usernameInput.fill(username);
  }

  // Enter password in the password field
  async enterPassword(password) {
    await this.passwordInput.clear();
    await this.passwordInput.fill(password);
  }

  // Click the login button
  async clickLogin() {
    await this.loginButton.click();
  }

  // Full login action combining all steps
  async login(username, password) {
    await this.enterUsername(username);
    await this.enterPassword(password);
    await this.clickLogin();
  }

  // Get error message text
  async getErrorMessage() {
    return await this.errorMessage.textContent();
  }

  // Check if error message is visible
  async isErrorVisible() {
    return await this.errorMessage.isVisible();
  }

  // Get the type attribute of the password field (to verify masking)
  async getPasswordFieldType() {
    return await this.passwordInput.getAttribute('type');
  }

  // Check if the login page is currently displayed
  async isLoginPageVisible() {
    return await this.loginButton.isVisible();
  }
}

module.exports = LoginPage;
```

---

=== FILE: pages/InventoryPage.js ===

```javascript
// InventoryPage.js - Page Object Model for the SauceDemo Inventory/Dashboard Page

class InventoryPage {
  constructor(page) {
    this.page = page;

    // Selectors using data-test attributes (preferred)
    this.inventoryContainer = page.locator('[data-test="inventory-container"]');
    this.pageTitle = page.locator('[data-test="title"]');
    this.appLogo = page.locator('.app_logo');
    this.menuButton = page.locator('#react-burger-menu-btn');
    this.logoutLink = page.locator('[data-test="logout-sidebar-link"]');
    this.shoppingCartBadge = page.locator('[data-test="shopping-cart-badge"]');
    this.inventoryItemList = page.locator('[data-test="inventory-item"]');

    // Expected URL pattern after login
    this.expectedUrl = 'https://www.saucedemo.com/inventory.html';
  }

  // Get the current page URL
  async getCurrentUrl() {
    return this.page.url();
  }

  // Get the page title text
  async getPageTitle() {
    return await this.pageTitle.textContent();
  }

  // Check if inventory container is visible (confirms successful login)
  async isInventoryVisible() {
    return await this.inventoryContainer.isVisible();
  }

  // Get the app logo text
  async getAppLogoText() {
    return await this.appLogo.textContent();
  }

  // Open the burger menu
  async openMenu() {
    await this.menuButton.click();
  }

  // Perform logout
  async logout() {
    await this.openMenu();
    await this.logoutLink.click();
  }

  // Get count of inventory items displayed
  async getInventoryItemCount() {
    return await this.inventoryItemList.count();
  }

  // Wait for inventory page to fully load
  async waitForPageLoad() {
    await this.inventoryContainer.waitFor({ state: 'visible' });
  }
}

module.exports = InventoryPage;
```

---

=== FILE: tests/login.spec.js ===

```javascript
// login.spec.js - Test suite for SauceDemo Login functionality
// URL: https://www.saucedemo.com
// Uses Page Object Model with LoginPage and InventoryPage

const { test, expect } = require('@playwright/test');
const LoginPage = require('../pages/LoginPage');
const InventoryPage = require('../pages/InventoryPage');

// NOTE: SauceDemo uses predefined usernames, not email addresses.
// The valid credentials for this site are:
//   Username: standard_user
//   Password: secret_sauce
// The test cases below use SauceDemo-compatible credentials
// while mapping to the user story requirements.

test.describe('Login Page - User Authentication Tests', () => {

  // ─────────────────────────────────────────────────────────────────────────
  // TEST CASE 1: Successful Login with Valid Credentials (Positive | High)
  // Verifies that a user with valid credentials can log in and is
  // redirected to the inventory/dashboard page.
  // ─────────────────────────────────────────────────────────────────────────
  test('TC01 - Successful login with valid credentials redirects to inventory page', async ({ page }) => {
    const loginPage = new LoginPage(page);
    const inventoryPage = new InventoryPage(page);

    // Step 1: Navigate to the login page
    await loginPage.navigate();

    // Step 2 & 3: Enter valid username and password
    await loginPage.enterUsername('standard_user');
    await loginPage.enterPassword('secret_sauce');

    // Step 4: Click the login button
    await loginPage.clickLogin();

    // Expected: User is redirected to the inventory/dashboard page
    await inventoryPage.waitForPageLoad();

    // Assert: URL has changed to the inventory page
    expect(page.url()).toBe('https://www.saucedemo.com/inventory.html');

    // Assert: Inventory container is visible (dashboard/home page loaded)
    await expect(inventoryPage.inventoryContainer).toBeVisible();

    // Assert: Page title reads "Products" (welcome/home page indicator)
    const title = await inventoryPage.getPageTitle();
    expect(title).toBe('Products');

    // Assert: App logo is visible
    await expect(inventoryPage.appLogo).toBeVisible();
  });

  // ─────────────────────────────────────────────────────────────────────────
  // TEST CASE 2: Login Fails with Incorrect Password (Negative | High)
  // Verifies that entering a correct username but wrong password
  // results in an error message and user stays on the login page.
  // ─────────────────────────────────────────────────────────────────────────
  test('TC02 - Login fails with incorrect password and shows error message', async ({ page }) => {
    const loginPage = new LoginPage(page);

    // Step 1: Navigate to the login page
    await loginPage.navigate();

    // Step 2: Enter valid username
    await loginPage.enterUsername('standard_user');

    // Step 3: Enter incorrect password
    await loginPage.enterPassword('WrongPass@999');

    // Step 4: Click the login button
    await loginPage.clickLogin();

    // Expected: Error message is displayed
    await expect(loginPage.errorMessage).toBeVisible();

    // Assert: Error message contains expected text about invalid credentials
    const errorText = await loginPage.getErrorMessage();
    expect(errorText).toContain('Username and password do not match');

    // Assert: User remains on the login page (URL unchanged)
    expect(page.url()).toBe('https://www.saucedemo.com/');

    // Assert: Login button is still visible (user not logged in)
    await expect(loginPage.loginButton).toBeVisible();
  });

  // ─────────────────────────────────────────────────────────────────────────
  // TEST CASE 3: Login Fails with Unregistered / Non-existent Username (Negative | High)
  // Verifies that using a username that doesn't exist in the system
  // returns an appropriate error message and blocks access.
  // ─────────────────────────────────────────────────────────────────────────
  test('TC03 - Login fails with unregistered username and shows error message', async ({ page }) => {
    const loginPage = new LoginPage(page);

    // Step 1: Navigate to the login page
    await loginPage.navigate();

    // Step 2: Enter a username that does not exist in the system
    await loginPage.enterUsername('notregistered_user');

    // Step 3: Enter any password
    await loginPage.enterPassword('AnyPass@456');

    // Step 4: Click the login button
    await loginPage.clickLogin();

    // Expected: Error message is displayed indicating invalid credentials
    await expect(loginPage.errorMessage).toBeVisible();

    // Assert: Error message contains relevant text
    const errorText = await loginPage.getErrorMessage();
    expect(errorText).toContain('Username and password do not match');

    // Assert: User remains on the login page
    expect(page.url()).toBe('https://www.saucedemo.com/');

    // Assert: Login form is still accessible
    await expect(loginPage.loginButton).toBeVisible();
  });

  // ─────────────────────────────────────────────────────────────────────────
  // TEST CASE 4: Login Fails with Empty Credentials (Negative | High)
  // Verifies that submitting the login form with both fields empty
  // triggers validation errors and blocks form submission.
  // ─────────────────────────────────────────────────────────────────────────
  test('TC04 - Login fails with empty credentials and shows validation error', async ({ page }) => {
    const loginPage = new LoginPage(page);

    // Step 1: Navigate to the login page
    await loginPage.navigate();

    // Step 2 & 3: Leave both username and password fields blank (do nothing)

    // Step 4: Click the login button without entering any credentials
    await loginPage.clickLogin();

    // Expected: Validation error message is shown
    await expect(loginPage.errorMessage).toBeVisible();

    // Assert: Error message indicates that username is required
    const errorText = await loginPage.getErrorMessage();
    expect(errorText).toContain('Username is required');

    // Assert: User remains on the login page
    expect(page.url()).toBe('https://www.saucedemo.com/');

    // Assert: Login button is still visible
    await expect(loginPage.loginButton).toBeVisible();
  });

  // ─────────────────────────────────────────────────────────────────────────
  // TEST CASE 5: Login with Username in Different Letter Cases (Edge Case | Medium)
  // Verifies the system's behavior when the username is entered in
  // uppercase. SauceDemo is case-sensitive, so this should fail or succeed
  // depending on the system's implementation.
  // ─────────────────────────────────────────────────────────────────────────
  test('TC05 - Login with username in uppercase returns appropriate response', async ({ page }) => {
    const loginPage = new LoginPage(page);

    // Step 1: Navigate to the login page
    await loginPage.navigate();

    // Step 2: Enter the username in ALL CAPS (edge case)
    await loginPage.enterUsername('STANDARD_USER');

    // Step 3: Enter the correct password
    await loginPage.enterPassword('secret_sauce');

    // Step 4: Click the login button
    await loginPage.clickLogin();

    // Expected: SauceDemo is case-sensitive, so
```

---

## 🎫 Jira Tickets Created
- ✅ SCRUM-48
- ✅ SCRUM-49
- ✅ SCRUM-50
- ✅ SCRUM-51
- ✅ SCRUM-52
- ✅ SCRUM-53
- ✅ SCRUM-54
- ✅ SCRUM-55

---
*Generated by QA Copilot Agent | github.com/Ahtsham-QA*
