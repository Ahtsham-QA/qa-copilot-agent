# QA Test Report
Generated: 2026-06-26 11:37:45
Generated by: QA Copilot Agent v2.0

---

## 📋 User Story
user can login with valid credentials

---

## 🧪 Test Cases
## TEST CASE 1: Successful Login with Valid Credentials
- Type: Positive
- Priority: High
- Preconditions: User has a registered account; application login page is accessible
- Test Data:
  * Email: john.carter@gmail.com
  * Password: SecurePass@123
- Steps:
  1. Navigate to the login page
  2. Enter "john.carter@gmail.com" in the Email field
  3. Enter "SecurePass@123" in the Password field
  4. Click the "Login" button
- Expected Result: User is successfully logged in and redirected to the dashboard/home page

---

## TEST CASE 2: Login Fails with Incorrect Password
- Type: Negative
- Priority: High
- Preconditions: User has a registered account; application login page is accessible
- Test Data:
  * Email: john.carter@gmail.com
  * Password: WrongPass@999
- Steps:
  1. Navigate to the login page
  2. Enter "john.carter@gmail.com" in the Email field
  3. Enter "WrongPass@999" in the Password field
  4. Click the "Login" button
- Expected Result: Login is rejected and an error message is displayed such as "Invalid email or password"

---

## TEST CASE 3: Login Fails with Unregistered Email
- Type: Negative
- Priority: High
- Preconditions: Application login page is accessible; email does not exist in the system
- Test Data:
  * Email: ghost.user@outlook.com
  * Password: AnyPass@456
- Steps:
  1. Navigate to the login page
  2. Enter "ghost.user@outlook.com" in the Email field
  3. Enter "AnyPass@456" in the Password field
  4. Click the "Login" button
- Expected Result: Login is rejected and an error message is displayed such as "Invalid email or password"

---

## TEST CASE 4: Login Fails with Empty Email and Password Fields
- Type: Negative
- Priority: High
- Preconditions: Application login page is accessible
- Test Data:
  * Email: (empty)
  * Password: (empty)
- Steps:
  1. Navigate to the login page
  2. Leave the Email field blank
  3. Leave the Password field blank
  4. Click the "Login" button
- Expected Result: Form submission is blocked and validation messages are shown such as "Email is required" and "Password is required"

---

## TEST CASE 5: Login with Password that is Case-Sensitive
- Type: Edge Case
- Priority: Medium
- Preconditions: User has a registered account; application login page is accessible
- Test Data:
  * Email: john.carter@gmail.com
  * Password: securepass@123
- Steps:
  1. Navigate to the login page
  2. Enter "john.carter@gmail.com" in the Email field
  3. Enter "securepass@123" in the Password field (all lowercase version of correct password)
  4. Click the "Login" button
- Expected Result: Login is rejected and an error message is displayed confirming passwords are case-sensitive

---

## TEST CASE 6: Login with Leading and Trailing Spaces in Email
- Type: Edge Case
- Priority: Medium
- Preconditions: User has a registered account; application login page is accessible
- Test Data:
  * Email: "   john.carter@gmail.com   " (with spaces)
  * Password: SecurePass@123
- Steps:
  1. Navigate to the login page
  2. Enter "   john.carter@gmail.com   " with leading and trailing spaces in the Email field
  3. Enter "SecurePass@123" in the Password field
  4. Click the "Login" button
- Expected Result: Application either trims the spaces and logs in successfully, or displays a clear validation error

---

## TEST CASE 7: Login with Valid Credentials After a Previously Failed Attempt
- Type: Positive
- Priority: Medium
- Preconditions: User has a registered account; application login page is accessible
- Test Data:
  * Email: john.carter@gmail.com
  * Password (first attempt): BadPass@000
  * Password (second attempt): SecurePass@123
- Steps:
  1. Navigate to the login page
  2. Enter "john.carter@gmail.com" in the Email field
  3. Enter "BadPass@000" in the Password field
  4. Click the "Login" button
  5. Observe the error message
  6. Re-enter "SecurePass@123" in the Password field
  7. Click the "Login" button again
- Expected Result: After the failed first attempt, the user is successfully logged in on the second attempt with the correct password and is redirected to the dashboard/home page

---

---

## 🎭 Playwright Tests
```javascript
=== FILE: pages/LoginPage.js ===

```javascript
// LoginPage.js - Page Object Model for the Sauce Demo Login Page

class LoginPage {
  constructor(page) {
    this.page = page;

    // Selectors using data-test attributes (preferred)
    this.usernameInput = page.locator('[data-test="username"]');
    this.passwordInput = page.locator('[data-test="password"]');
    this.loginButton = page.locator('[data-test="login-button"]');
    this.errorMessage = page.locator('[data-test="error"]');
    this.errorMessageContainer = page.locator('.error-message-container');
  }

  // Navigate to the login page
  async navigate() {
    await this.page.goto('https://www.saucedemo.com');
  }

  // Enter username in the username field
  async enterUsername(username) {
    await this.usernameInput.clear();
    await this.usernameInput.fill(username);
  }

  // Enter password in the password field
  async enterPassword(password) {
    await this.passwordInput.clear();
    await this.passwordInput.fill(password);
  }

  // Click the login button
  async clickLoginButton() {
    await this.loginButton.click();
  }

  // Complete login action with username and password
  async login(username, password) {
    await this.enterUsername(username);
    await this.enterPassword(password);
    await this.clickLoginButton();
  }

  // Get error message text
  async getErrorMessage() {
    return await this.errorMessage.textContent();
  }

  // Check if error message is visible
  async isErrorMessageVisible() {
    return await this.errorMessage.isVisible();
  }
}

module.exports = LoginPage;
```

---

=== FILE: pages/InventoryPage.js ===

```javascript
// InventoryPage.js - Page Object Model for the Sauce Demo Inventory Page

class InventoryPage {
  constructor(page) {
    this.page = page;

    // Selectors using data-test attributes (preferred)
    this.inventoryContainer = page.locator('[data-test="inventory-container"]');
    this.inventoryList = page.locator('[data-test="inventory-list"]');
    this.pageTitle = page.locator('[data-test="title"]');
    this.shoppingCartIcon = page.locator('[data-test="shopping-cart-link"]');
    this.burgerMenuButton = page.locator('[data-test="open-menu"]');
    this.inventoryItems = page.locator('[data-test="inventory-item"]');
    this.appLogo = page.locator('.app_logo');
  }

  // Check if inventory page is loaded
  async isLoaded() {
    return await this.inventoryContainer.isVisible();
  }

  // Get the page title text
  async getPageTitle() {
    return await this.pageTitle.textContent();
  }

  // Get the count of inventory items displayed
  async getInventoryItemCount() {
    return await this.inventoryItems.count();
  }

  // Check if shopping cart is visible
  async isShoppingCartVisible() {
    return await this.shoppingCartIcon.isVisible();
  }

  // Click the burger menu
  async openBurgerMenu() {
    await this.burgerMenuButton.click();
  }
}

module.exports = InventoryPage;
```

---

=== FILE: tests/login.spec.js ===

```javascript
// login.spec.js - Playwright test suite for Sauce Demo login functionality

const { test, expect } = require('@playwright/test');
const LoginPage = require('../pages/LoginPage');
const InventoryPage = require('../pages/InventoryPage');
const testData = require('../test-data/test_data.json');

// ─────────────────────────────────────────────
// VALID LOGIN TESTS - Loop through valid_data
// ─────────────────────────────────────────────

testData.valid_data.forEach((data) => {
  test(`[Positive] Valid Login: ${data.description}`, async ({ page }) => {
    // Arrange: Initialize page objects
    const loginPage = new LoginPage(page);
    const inventoryPage = new InventoryPage(page);

    // Act: Navigate to login page and perform login
    await loginPage.navigate();
    await loginPage.login(data.username, data.password);

    // Assert: User should be redirected to the inventory/dashboard page
    await expect(page).toHaveURL(/inventory/);

    // Assert: Inventory container should be visible after successful login
    await expect(inventoryPage.inventoryContainer).toBeVisible();

    // Assert: Page title should display "Products"
    await expect(inventoryPage.pageTitle).toHaveText(data.expected_title);
  });
});

// ─────────────────────────────────────────────
// INVALID LOGIN TESTS - Loop through invalid_data
// ─────────────────────────────────────────────

testData.invalid_data.forEach((data) => {
  test(`[Negative] Invalid Login: ${data.description}`, async ({ page }) => {
    // Arrange: Initialize page objects
    const loginPage = new LoginPage(page);

    // Act: Navigate to login page and attempt login with invalid credentials
    await loginPage.navigate();
    await loginPage.login(data.username, data.password);

    // Assert: URL should remain on the login page (no redirect)
    await expect(page).toHaveURL('https://www.saucedemo.com/');

    // Assert: Error message should be visible
    await expect(loginPage.errorMessage).toBeVisible();

    // Assert: Error message should contain expected text from test data
    await expect(loginPage.errorMessage).toContainText(data.expected_error);
  });
});

// ─────────────────────────────────────────────
// EDGE CASE TESTS - Loop through edge_cases array
// ─────────────────────────────────────────────

testData.edge_cases.forEach((data) => {
  test(`[Edge Case] ${data.description}`, async ({ page }) => {
    // Arrange: Initialize page objects
    const loginPage = new LoginPage(page);
    const inventoryPage = new InventoryPage(page);

    // Act: Navigate to login page
    await loginPage.navigate();

    // Handle retry scenario - first attempt with bad credentials
    if (data.first_password) {
      // Step 1: Attempt login with the bad/first password
      await loginPage.login(data.username, data.first_password);

      // Assert: First attempt should fail with an error message
      await expect(loginPage.errorMessage).toBeVisible();
      await expect(loginPage.errorMessage).toContainText(data.expected_first_error);

      // Step 2: Re-enter the correct password and attempt login again
      await loginPage.login(data.username, data.password);

      // Assert: Second attempt should succeed - redirect to inventory
      await expect(page).toHaveURL(/inventory/);
      await expect(inventoryPage.inventoryContainer).toBeVisible();
      await expect(inventoryPage.pageTitle).toHaveText(data.expected_title);

    } else if (data.expected_outcome === 'success') {
      // Edge case: login expected to succeed (e.g., spaces trimmed in email)
      await loginPage.login(data.username, data.password);

      // Assert: User should be redirected to inventory page
      await expect(page).toHaveURL(/inventory/);
      await expect(inventoryPage.inventoryContainer).toBeVisible();

    } else {
      // Edge case: login expected to fail (e.g., wrong case password)
      await loginPage.login(data.username, data.password);

      // Assert: URL should remain on login page
      await expect(page).toHaveURL('https://www.saucedemo.com/');

      // Assert: Error message should be visible
      await expect(loginPage.errorMessage).toBeVisible();

      // Assert: Error message should match expected error from test data
      await expect(loginPage.errorMessage).toContainText(data.expected_error);
    }
  });
});
```

---

=== FILE: test-data/test_data.json ===

```json
{
  "valid_data": [
    {
      "description": "Successful Login with Valid Credentials (standard_user)",
      "username": "standard_user",
      "password": "secret_sauce",
      "expected_title": "Products"
    }
  ],

  "invalid_data": [
    {
      "description": "Login Fails with Incorrect Password",
      "username": "standard_user",
      "password": "WrongPass@999",
      "expected_error": "Username and password do not match any user in this service"
    },
    {
      "description": "Login Fails with Unregistered Email",
      "username": "ghost.user@outlook.com",
      "password": "AnyPass@456",
      "expected_error": "Username and password do not match any user in this service"
    },
    {
      "description": "Login Fails with Empty Email and Password Fields",
      "username": "",
      "password": "",
      "expected_error": "Username is required"
    }
  ],

  "edge_cases": [
    {
      "description": "Login with Password that is Case-Sensitive (lowercase password)",
      "username": "standard_user",
      "password": "SECRET_SAUCE",
      "expected_outcome": "failure",
      "expected_error": "Username and password do not match any user in this service"
    },
    {
      "description": "Login with Leading and Trailing Spaces in Username",
      "username": "   standard_user   ",
      "password": "secret_sauce",
      "expected_outcome": "failure",
      "expected_error": "Username and password do not match any user in this service"
    },
    {
      "description": "Login with Valid Credentials After a Previously Failed Attempt",
      "username": "standard_user",
      "first_password": "BadPass@000",
      "password": "secret_sauce",
      "expected_first_error": "Username and password do not match any user in this service",
      "expected_outcome": "success",
      "expected_title": "Products"
    }
  ]
}
```
```

---

## 🎫 Jira Tickets Created
- ✅ SCRUM-64
- ✅ SCRUM-65
- ✅ SCRUM-66
- ✅ SCRUM-67
- ✅ SCRUM-68
- ✅ SCRUM-69
- ✅ SCRUM-70

---
*Generated by QA Copilot Agent | github.com/Ahtsham-QA*
